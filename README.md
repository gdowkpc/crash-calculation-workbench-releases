# Crash Calculation Workbench Releases

This is a release-only distribution repository for Crash Calculation Workbench.

It intentionally contains no application source code, source history, tests, build scripts, secrets, local configuration, CVS data, or NHTSA datasets.

Use this repository only for GitHub Releases, compiled portable/installer artifacts, checksums, release notes, and update manifests.
